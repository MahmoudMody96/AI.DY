// Re-export — the canonical implementation lives in `@/components/categories/category-card`.
// Kept here for backward compatibility with any legacy import.
export {
  CategoryCard,
  type CategoryCardData,
} from "@/components/categories/category-card";
