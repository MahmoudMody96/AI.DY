// Re-export — the canonical implementation lives in `@/components/tools/tool-grid`.
// Kept here as a thin re-export so existing page imports (`./tool-grid`,
// `../tool-grid`) keep working after the refactor.
export { ToolGrid } from "@/components/tools/tool-grid";
